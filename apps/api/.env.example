# Hugging Face token — free at https://huggingface.co/settings/tokens (read access is enough).
# Never exposed to the browser: every AI call goes through this backend.
HF_TOKEN=

# Optional overrides
# HF_T2I_MODEL=black-forest-labs/FLUX.1-schnell
# HF_I2I_MODEL=timbrooks/instruct-pix2pix
# AI_TIMEOUT_MS=60000
# RUN_CONCURRENCY=4
# PORT=3001
